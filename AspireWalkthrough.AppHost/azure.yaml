# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aspirewalkthrough-apphost
services:  
  app:
    language: dotnet
    project: ./AspireWalkthrough.AppHost.csproj
    host: containerapp
